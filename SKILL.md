---
name: cultural-tourism-wiki
description: 文旅知识库摄取与多视角生成 Skill。当用户上传老照片、导览图、文献截图、口述史、PDF、网页链接，或要求「归档/整理/建库/建 Wiki 页面/生成儿童·历史·专家三视角解读/更新索引与日志/做知识库体检」时使用。也适用于「这条史料该归到哪一类」「这两条是不是重复」等维护性问题。基于 Karpathy LLM Wiki 的「知识编译」范式——原始资料只读、Wiki 增量编译、一次编译长期复用，而非每次查询临时检索。
agent_created: true
---

# 文旅知识库摄取与多视角生成

把零散的文旅史料（图片 / 文本 / PDF / 网页 / 口述史）**编译**成一份可长期生长的 Markdown Wiki，并为每条知识生成 **儿童 / 历史 / 专家** 三种视角的解读。

核心范式（Karpathy LLM Wiki）：

> 知识被编译一次，然后保持更新，而不是每次查询时重新推导。
> Obsidian 是 IDE，LLM 是程序员，Wiki 是代码库。

## 三层架构（不可混淆）

| 层 | 位置 | 谁写 | 谁读 | 规则 |
|---|---|---|---|---|
| 原始资料 | `wiki/raw/` | 用户 / 本 Skill 落盘 | LLM | **只读**。一旦保存永不修改、永不删除，是可追溯的证据源 |
| 知识库 | `wiki/pages/` `index.md` `log.md` | **LLM 全权维护** | 用户 / Dify 检索 | 可增删改，但每次改动必须留痕到 `log.md` |
| 规范 | 本 Skill + `wiki/_config.md` | 共同演进 | LLM | 项目专属约定写进 `_config.md`，不写进 Skill |

## 目录结构

```
wiki/
├── _config.md              # 项目配置：项目名、视角集合、分类、人称
├── index.md                # 内容索引（按分类，脚本可重建）
├── log.md                  # 时间线日志（append-only）
├── overview.md             # 可选：全局总览/导读
├── raw/                    # 原始资料（只读）
│   ├── images/             #   图片，命名 YYYYMMDD-HHMMSS-描述.jpg
│   ├── texts/              #   粘贴或 OCR 提取的文本
│   └── files/              #   PDF、网页存档
├── pages/
│   ├── entities/           # 实体（香山银行、风水古井）
│   ├── events/             # 事件（1948年重建开业）
│   ├── buildings/          # 建筑（钟楼、骑楼外立面）
│   ├── people/             # 人物（郭泗泉、李德联）
│   └── topics/             # 主题（修缮工艺、客房定价）
└── lint-report.md          # 体检报告（脚本生成）
```

多视角内容默认**内嵌**在页面 `## 多视角解读` 章节，不单独建 `perspectives/` 目录——这样 Dify 分段检索时视角与事实不会被切散。

## 命令路由（也接受自然语言）

| 意图 | 动作 | 说明 |
|---|---|---|
| 建库 / 初始化 | `/wiki init` | 建骨架与 `_config.md` |
| 收到一份新资料 | `/wiki ingest` | 归档 → 识别 → 编译 → 三视角 → 更新索引日志 |
| 只补视角 / 重写视角 | `/wiki lens <页面>` | 不动事实底座，只重写视角章节 |
| 查知识库 | `/wiki query <问题>` | 先读 `index.md`，再下钻页面，答案可回存为新页 |
| 体检 | `/wiki lint` | 断链、孤儿、缺视角、冲突、重复、过时 |

自然语言等价说法：「收录这张照片」「整理一下这段资料」「给钟楼写个儿童版」「检查下知识库」「这份史料该放哪」——按意图路由，不要反问用户选命令。

## 标准摄取流程（ingest）

1. **落盘原始资料** → `raw/` 对应子目录，按命名规范，写 `log.md`。
2. **识别与提取**：图片走 OCR / 视觉模型，文本做清洗分段，输出「原始内容摘要」（核心主题 / 时间 / 地点 / 人物 / 事件 / 候选分类）。
3. **写事实底座**：先把可溯源的事实清单写进页面 `## 事实底座`，**每条事实带 `<!-- src: raw/... -->` 锚点**。
4. **新建 or 合并判断**：先查 `index.md` + 页面别名（见 `references/ingest-workflow.md` 的决策树）。**默认合并，新建是例外**。
5. **生成三视角**：严格基于事实底座，不引入底座外的信息。
6. **更新索引与日志**：`index.md` 用脚本重建，日志手写追加。
7. **输出报告**：资料清单 / 页面清单 / 视角数 / 需复核项 / 下一步。

## 硬规则（违反即返工）

1. **禁止编造。** 页面里任何一句事实都必须能在 `raw/` 里找到出处。没有出处 = 不写，或写进「待补充」并标 `[需人工复核]`。
2. **保留细节。** 地址、年代、人名、建筑构件、尺寸、价格等一律原样保留，不得概括合并。
3. **先事实，后视角。** 三视角共享同一个 `## 事实底座`，只换表达方式，不换事实。
4. **原始资料只读。** 绝不编辑、重命名、删除 `raw/` 下任何文件。
5. **增量优先。** 一份资料通常要动 5–15 个页面（交叉引用、主题页、时间线），不要只建一个页面就收工。
6. **关键事实标复核。** 图片 OCR 结果、年代、人名、数字，自动识别的一律标 `[需人工复核]`。
7. **幂等。** 同一份资料重复提交时，先比对内容哈希，已摄取过就合并而非重复建页。

## 脚本（优先用脚本，别手搓索引）

```bash
PY="C:/Users/26977/.workbuddy/binaries/python/versions/3.13.12/python.exe"
S="C:/Users/26977/.workbuddy/skills/cultural-tourism-wiki/scripts"

"$PY" "$S/wiki_init.py"  --root wiki --project "思豪大酒店"   # 初始化骨架
"$PY" "$S/wiki_index.py" --root wiki                          # 重建 index.md
"$PY" "$S/wiki_lint.py"  --root wiki --out wiki/lint-report.md # 体检
```

脚本为纯标准库、Windows 可直接运行。索引与体检**必须**用脚本，LLM 手写索引必然会和实际文件漂移。

## 参考文档（按需加载）

- `references/schema.md` — 页面模板、frontmatter 字段、分类法、命名与溯源规范
- `references/lenses.md` — 三视角生成规范与一致性校验
- `references/ingest-workflow.md` — 摄取七步详解、合并决策树、冲突与去重、批量策略
- `references/lint.md` — 体检清单与报告格式
- `references/dify-integration.md` — Dify 打包上传、图片方案、Chatflow 衔接、成本控制
- `assets/page-template.md` / `index-template.md` / `log-template.md` — 可直接复制的模板

## 首次使用

若 `wiki/` 不存在或用户尚未建库，先跑 `wiki_init.py`，并**向用户确认**项目名、视角集合（默认 儿童/历史/专家）、分类（默认五类），写入 `wiki/_config.md`。确认后即可开始摄取。
